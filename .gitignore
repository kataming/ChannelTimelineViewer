# 秘密情報（実APIキー）。example のみコミットする。
Config.plist
Resources/Config.plist
# APIキー等を書いたメモ/ローカルファイル（誤コミット防止）
youtubeapikey.txt
*apikey*.txt
*api_key*.txt
*.secret
# 署名アセット（配布証明書の秘密鍵 / .p12 / ASC APIキー / プロファイル）は絶対にコミットしない
build/signing/
*.p12
*.p8
*.mobileprovision
*.key.pem
# ローカルの企画・差別化メモ（公開リポジトリに含めない）
docs/差別化.txt

# XcodeGen が生成するプロジェクト
*.xcodeproj

# Xcode / ビルド成果物
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.swiftpm/
.build/

# macOS
.DS_Store

# Claude Code などのローカルツール状態
.claude/
# 構築時の内部やり取り・Claude 用ローカル文脈（公開リポジトリに含めない）
CLAUDE.md
docs/構築チャット履歴.md

# Python のキャッシュ
__pycache__/
*.pyc

# --- dev-backup 監査(2026-09-25)で追加: 秘密・依存・ローカル状態は Git に入れない ---
.env
.env.*
*.pem
*.key
.dev.vars
node_modules/
.wrangler/
*.jks
*.keystore
!android/app/debug.keystore
!.env.example
